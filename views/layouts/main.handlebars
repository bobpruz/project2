<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Detroit Public Library</title>
  <link rel="stylesheet" href="/style.css">
</head>

<body>
  <div class="wrapper">
    <header>
      <h1>
        <a href="/">Detroit Public Library</a>
      </h1>
      <nav>
        {{#if loggedIn}}
        <a href="/account">Account</a>
        <button id="logout" class="btn-no-style">logout</button>
        {{else}}
        <a href="/login">login</a>
        {{/if}}
      </nav>
    </header>
    <main>
      {{{ body }}}
    </main>
    <footer>
      Keep on Reading!
    </footer>
  </div>
  {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
  {{/if}}
</body>

</html>