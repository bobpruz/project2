<div>
    {{> book-info book }}
    
    {{#if loggedIn}}
        <button type="button" class="btn btn-primary" data-book_id="{{book.id}}">Checkout Book</button>
        <form class="review-form">
            <div>
                <textarea name="review-body"></textarea>
            </div>
    
            <div>
                <button type="submit">add review</button>
            </div>
        </form>
    {{/if}}
    
    {{> review book.reviews}}
</div>

    <script src="/javascript/reviews.js"></script>
